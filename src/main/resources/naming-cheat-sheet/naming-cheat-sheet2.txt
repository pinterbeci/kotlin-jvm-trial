HTTP / REST / Controllers

handle — process a request or event.
route — direct requests to handlers.
map — convert one representation to another (DTO ↔ domain).
bind — attach request data to a model.
decode — convert a payload from an encoded format.
encode — convert to a target format (e.g., JSON, Base64).
parse — read structured text into objects.
render — generate a response body.
respond — return HTTP response.
redirect — send the client to another URL.
forward — pass processing to another handler internally.
negotiate — content negotiation (Accept/Content-Type).
paginate — split results into pages.
filter — apply criteria to results.
sort — order the results.
validate — ensure request data meets constraints.
authorize — enforce permissions on an action.
authenticate — verify identity/credentials.
rateLimit — restrict number of requests.
throttle — reduce processing frequency.
debounce — wait for quiet period before processing.
stream — send/receive chunked data.
upload — receive client data.
download — send file to client.
compress — reduce size of payload (gzip).
decompress — expand compressed payload.
cache — store response for reuse.
invalidate — mark cached response stale.


Spring / DI / Configuration

configure — set up beans/options.
register — add bean/service to container.
unregister — remove from container.
inject — provide dependency instance.
autowire — let container resolve dependencies.
qualify — select among multiple candidates.
profile — activate environment-specific configuration.
componentScan — discover annotated components.
postConstruct — run after bean initialization.
preDestroy — run before bean destruction.
refresh — reload application context/config.
bindProperties — map config to POJOs/data classes.
override — replace default config/value.
schedule — execute at defined intervals (cron/fixed rate).
transactional — wrap method in DB transaction.


Kotlin Coroutines / Reactive

launch — start a coroutine.
async — start concurrent task that returns a Deferred.
await — suspend until result is ready.
cancel — stop coroutine/task.
supervise — manage child coroutine failures.
withContext — switch dispatcher/context.
delay — suspend for a time.
yield — let other coroutines run.
flow — represent cold asynchronous stream.
emit — produce a value in a Flow.
collect — consume values from a Flow.
onEach — side-effect per item in stream.
debounce — emit after inactivity window.
throttleFirst — emit first then suppress for window.
buffer — add capacity to flow/channel.
channel — send/receive messages.
send — push to channel.
receive — pull from channel.
select — wait on multiple suspending ops.


Persistence / JPA / Repositories

persist — save new entity (managed).
merge — copy state of detached entity into persistence context.
detach — remove entity from persistence context.
flush — write pending changes to DB.
clear — clear persistence context.
remove — delete entity.
find — load by identifier.
getReference — lazy proxy ref by ID.
save — store new/updated entity (Spring Data).
update — change existing entity data.
delete — remove record.
upsert — insert or update.
query — retrieve matching records.
specify — build criteria/specification.
paginate — fetch page via offset/limit.
index — create/index for faster lookup.
lock — obtain DB lock for concurrency control.
unlock — release lock.
optimisticLock — check version for conflicts.
pessimisticLock — block others while operating.


SQL / Migrations

migrate — apply DB schema changes.
rollback — revert to previous version.
seed — insert initial/fake data.
version — set/check migration version.
checkpoint — store state for recovery.
diff — compute schema differences.
verifyChecksum — validate migration file integrity.


Messaging / Events (Kafka, RabbitMQ, etc.)

publish — send event/message.
subscribe — listen to topic/queue.
produce — write to topic/stream.
consume — read from topic/queue.
acknowledge — confirm message processed.
nack — negative acknowledgement.
retry — reprocess failed message.
deadLetter — route unprocessable messages.
route — direct messages to destination.
partition — divide stream by key.
offset — track read position.
commit — persist read position/state.
seek — reposition consumer offset.


Caching

cache — store result for reuse.
memoize — cache function result per input.
evict — remove item from cache.
invalidate — mark stale.
refresh — reload cache contents.
warm — pre-populate frequently used entries.
hydrate — load full state from store into cache.
dehydrate — reduce/serialize state for storage.


Security / Auth

authenticate — verify identity (credentials).
authorize — enforce permissions/roles.
challenge — initiate auth flow (e.g., 401).
hash — compute digest (e.g., SHA‑256).
salt — add randomness before hashing.
encrypt — protect data with key.
decrypt — restore plaintext from ciphertext.
sign — produce digital signature.
verifySignature — check signature authenticity.
generateToken — issue access/refresh token.
validateToken — check token expiry/claims.
rotateKey — replace cryptographic keys.
revokeToken — invalidate a token/session.
mask — hide sensitive parts in output.
redact — remove sensitive data entirely.
obfuscate — make data/code harder to interpret.


Observability / Monitoring

log — record structured events.
trace — track request path across services.
debug — produce detailed diagnostic info.
info — general runtime information.
warn — notable issue not causing failure.
error — record failures.
audit — record compliance-relevant activity.
monitor — observe health/metrics.
instrument — add metrics to code paths.
sample — limit telemetry volume.
aggregate — compute metric summaries.
profile — measure performance characteristics.


Errors / Resilience

try — attempt operation with exception handling.
catch — handle exceptions.
throw — raise an exception.
rethrow — propagate after handling.
wrap — convert exception type.
unwrap — extract root cause.
mapError — transform failures to domain errors.
recover — provide alternative result on failure.
fallback — use alternative path/resource.
retry — execute again under policy.
timeout — fail if not completed in time.
circuitBreak — stop calls on repeated failures.
bulkhead — isolate resources to contain failures.
shedLoad — reject excess requests under pressure.


Data Processing / Collections

transform — change structure/content.
convert — change type/representation.
translate — map between domains/encodings.
normalize — standardize representation.
canonicalize — make canonical form.
sanitize — remove unsafe input.
scrub — thoroughly clean data.
redact — remove sensitive segments.
mask — partially hide data.
map — apply function to each element.
flatMap — map then flatten.
reduce — combine to single result.
fold — reduce with accumulator.
group — cluster by key.
aggregate — summarize data.
distinct — unique elements only.
deduplicate — remove duplicates.
partition — split by predicate.
chunk — split into fixed sizes.
zip — pair elements from two sequences.
unzip — separate pairs into sequences.
filter — keep items matching predicate.
sort — order elements.
shuffle — randomize order.


Files / Streams / I/O

open — begin using resource/stream.
close — release resource.
read — load data from stream/file.
write — output data to stream/file.
append — add to end.
flush — push buffered output.
fsync — force write to disk.
lockFile — obtain file lock.
unlockFile — release file lock.
stream — process as flowing data.
pipe — connect streams.
serialize — convert object to bytes/string.
deserialize — reconstruct object from bytes/string.
marshal — package for transport.
unmarshal — rebuild from marshalled data.


Build / Deployment / Ops

compile — build source to bytecode.
package — assemble artifact (jar/war).
publish — push artifact to repository.
deploy — release to environment.
provision — prepare infrastructure/services.
configure — set environment-specific params.
scale — adjust replicas/resources.
restart — stop/start service.
shutdown — terminate service gracefully.
rollback — revert a deployment.
healthCheck — verify service health.
reload — refresh configuration without restart.


Configuration / Feature Flags

loadConfig — read settings from store.
resolve — compute final value (overrides).
override — replace defaults.
bind — attach config to object.
validateConfig — ensure config is correct.
enable — turn feature on.
disable — turn feature off.
toggle — switch feature state.
expose — make configuration visible (if safe).


Schedulers / Jobs / Batch

enqueue — add job to queue.
dequeue — remove job from queue.
schedule — plan execution time.
reschedule — change planned time.
run — execute unit of work.
execute — perform task logic.
trigger — start based on event or time.
cancel — stop in-flight job.
retry — reattempt job.
backoff — delay retries progressively.


GraphQL / gRPC (if applicable)

resolveField — compute value for a field.
fetch — load data for resolver.
alias — map field names.
deprecate — mark field to be phased out.
stream — server-side streaming (gRPC).
unary — single request-single response (gRPC).


Testing / QA

mock — create test double with behavior.
stub — provide canned responses.
spy — partial mock to verify interactions.
assert — check expected conditions.
expect — specify desired outcome.
verify — confirm interactions/have been called.
capture — record arguments passed to mocks.
parameterize — run test with multiple inputs.
fixture — set up reusable test data/environment.