Validation & Checks

validate — ensure data conforms to rules.
verify — confirm correctness (often via external check).
ensure — enforce a condition or precondition.
assert — hard check that throws on failure (for tests or invariants).
check — general-purpose condition check.
require — precondition that throws if unmet.
enforce — apply a rule and fail if violated.
guard — protect against invalid states/inputs.
constrain — limit values to a permitted range/domain.
sanitize — clean unsafe input (e.g., strip harmful characters).
normalize — convert to canonical/consistent form.
canonicalize — produce a single standard representation.
deduplicate — remove duplicates.
filter — keep only items meeting criteria.
reject — drop items failing criteria.
approve — mark/accept as valid.
qualify — check that criteria are met to proceed.
whitelist / allowlist — permit only the listed items.
blacklist / denylist — block the listed items.
validateSchema — check structure against a schema.
lint — check style or static rules and report issues.
sanitizeHtml — remove unsafe HTML/script content.
mask — hide sensitive fields (e.g., show only last 4 digits).
redact — remove/obscure sensitive information.
scrub — remove unwanted/sensitive data thoroughly.


Transformation & Formatting

convert — change type or representation.
transform — change shape/structure/content.
translate — convert between domains/encodings.
cast — reinterpret type (with/without safety).
map — apply a function to each element.
flatMap — map and flatten results.
format — produce human-readable text layout.
parse — read structured text into objects.
prettify — beautify output (spacing, naming).
minify — remove whitespace/comments for compact output.
encode — convert to a specific encoding (e.g., Base64, URL).
decode — revert from an encoding back to original.
serialize — turn an object into a byte/string representation.
deserialize — reconstruct object from serialized data.
marshal — package for transport or interop.
unmarshal — reconstruct from marshalled data.
compress — reduce size via compression.
decompress — restore compressed data.
tokenize — split input into tokens.
trim — remove leading/trailing whitespace.
capitalize — uppercase first letter or words.
slugify — produce URL-safe slugs from strings.


Retrieval & Access

get — retrieve a value/property.
fetch — retrieve (often remote or async).
obtain — acquire a resource or value.
retrieve — get previously stored content.
read — read from a source/stream/file.
load — bring data into memory from storage.
resolve — map an identifier to a resource/value.
lookup — find by key/index/registry.
list — enumerate resources/items.
query — request matching records/data via criteria.
scan — read sequentially to inspect or find.
search — look for matches across data.
enumerate — list with indexes/identifiers.
index — build or access lookup structures.
peek — inspect without consuming.
inspect — look into structure/metadata.


Creation & Construction

create — new instance/resource.
build — assemble with configuration.
construct — form from parts.
initialize — set initial state.
instantiate — create object from class.
spawn — start a new process/task/entity.
allocate — reserve resources/memory.
provision — prepare resources/services for use.
generate — produce new content/IDs/data.
derive — compute based on existing inputs.
compose — assemble from components.
clone — copy an existing instance.
copy — duplicate data/state.
bootstrap — start with minimal setup.


Update & Mutation

set — assign a value to a property.
update — change to a newer state/value.
mutate — alter internal state.
modify — change part(s) of a value.
change — generic alteration.
replace — substitute entirely.
swap — exchange two values.
toggle — switch between states.
increment — increase by one/step.
decrement — decrease by one/step.
append — add to the end.
prepend — add to the beginning.
merge — combine data/collections.
upsert — insert or update depending on existence.
patch — apply a partial update.
apply — apply a configuration/update.
promote — move to a higher stage/priority.
demote — move to a lower stage/priority.


Removal, Cleanup & Reset

delete — remove permanently.
remove — detach from collection or state.
purge — thoroughly remove (including caches).
clear — empty contents/reset to blank state.
reset — revert to initial defaults.
discard — drop without preserving.
drop — remove resource/table/item.
revoke — withdraw granted permissions/tokens.
unlink — detach association/reference.
destroy — tear down permanently.
teardown — dismantle environment/resources.
close — finish and free associated resources.
cleanup — tidy up residual artifacts/resources.
invalidate — mark as no longer valid (cache/session).
evict — remove from cache/memory.


Aggregation, Math & Ordering

sum — total numeric values.
average — mean of values.
aggregate — combine multiple items into one summary.
fold — reduce with accumulator.
reduce — combine into a single result.
group — cluster by key or property.
count — number of items.
tally — cumulative count.
compute — calculate result.
calculate — perform arithmetic/logic.
evaluate — compute/assess expression or rule.
estimate — approximate value.
measure — determine size/duration.
rank — order by score/priority.
sort — order ascending/descending.
order — arrange by criteria.
compare — determine relative ordering/equality.
normalizeScale — adjust to common scale/range.


Collections & Sequences

add — insert an item.
insert — put at a specific position.
push — add at end (stack/list).
pop — remove from end and return.
shift — remove from beginning and return.
unshift — add to beginning.
join — concatenate elements.
split — divide into parts.
slice — take a subrange.
splice — add/remove within a range.
partition — split by predicate.
chunk — group into fixed-size blocks.
zip — combine two sequences element-wise.
unzip — separate paired sequence.
flatten — collapse nested collections.
distinct — keep unique items.


Concurrency & Scheduling

launch — start a coroutine/task.
execute — run a unit of work.
run — perform a block/function.
schedule — plan future execution.
await — suspend until result is ready.
cancel — stop in-progress work.
abort — terminate immediately.
retry — reattempt on failure.
debounce — delay until quiet period.
throttle — limit execution rate.
synchronize — coordinate shared state access.
lock — acquire exclusive access.
unlock — release lock.
yield — give up CPU/control voluntarily.
batch — group tasks to process together.


I/O, Files & Networking

open — begin using a resource/stream.
close — end usage and release resources.
read — input from file/stream.
write — output to file/stream.
appendFile — add to file end.
flush — ensure buffered output is written.
stream — process as a continuous flow.
connect — establish network link.
disconnect — terminate link.
send — transmit data to peer.
receive — accept incoming data.
publish — make data/events available.
subscribe — listen for events/data.
upload — send from local to remote.
download — fetch from remote to local.
mount — attach filesystem/resource.
unmount — detach filesystem/resource.
resolveDns — find IP via DNS.


Security, Privacy & Auth

authenticate — verify identity (credentials).
authorize — check permissions/roles.
sign — produce digital signature.
verifySignature — confirm signature validity.
encrypt — make data unreadable without key.
decrypt — restore encrypted data.
hash — produce fixed-size digest.
salt — add randomness for secure hashing.
mask — hide sensitive parts (e.g., PAN).
obfuscate — make code/data harder to interpret.
scramble — rearrange data to conceal meaning.
redact — remove or blur sensitive segments.
rotateKey — replace cryptographic keys.
renewToken — issue new access token.
revokeToken — invalidate a token.


Configuration & Dependency Management

configure — set up options/parameters.
setup — establish initial configuration.
enable — turn feature on.
disable — turn feature off.
tune — adjust performance parameters.
calibrate — align settings to benchmarks.
parameterize — make configurable via parameters.
override — replace default/parent value.
inject — provide dependency (DI).
bind — associate interface to implementation.
register — add to registry/container.
unregister — remove from registry.
resolveDependency — find/provide required services.


Errors, Resilience & Instrumentation

try — attempt operation (exception handling).
catch — handle thrown exceptions.
throw — raise an exception.
raise — alternative term for throw.
fail — mark operation unsuccessful.
recover — restore from error state.
fallback — alternative behavior if primary fails.
compensate — undo effects (saga pattern).
timeout — end operation after limit.
circuitBreak — stop calls on repeated failures.
log — record events/messages.
trace — detailed execution steps.
debug — produce diagnostic output.
profile — measure performance characteristics.
audit — record compliance-relevant events.
monitor — observe metrics/health.
instrument — add metrics/telemetry.


Caching & Persistence

cache — store for fast reuse.
memoize — cache function results by input.
persist — store durably (DB/disk).
save — write to storage.
store — place into storage system.
commit — finalize transaction.
rollback — revert transaction.
checkpoint — save intermediate state.
snapshot — capture point-in-time state.
flush — write buffered changes (DB/cache).
invalidate — mark cached data stale.
evict — remove from cache.
loadFromCache — attempt to use cached value.


Lifecycle & State

start — begin operation/lifecycle.
stop — end operation/lifecycle.
pause — temporarily halt.
resume — continue after pause.
initialize — set initial state.
finalize — perform end-of-life actions.
dispose — clean up resources.
shutdown — orderly stop of system/service.
resetState — revert to defaults.
freeze — make object immutable.
readOnly — make accessible without mutation (or return read-only view).


Domain/Business-Oriented (generic)

calculatePrice — compute cost given rules.
settle — finalize an order/transaction.
reconcile — match records to resolve differences.
allocateBudget — assign funds to categories.
fulfill — execute/complete a request/order.
authorizePayment — pre-approve transaction.
capturePayment — finalize and charge.
refund — return funds.
scheduleDelivery — plan fulfillment timing.
validateOrder — ensure order meets constraints.